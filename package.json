{
	"devDependencies": {
		"@biomejs/biome": "2.4.6"
	},
	"name": "magic3t",
	"scripts": {
		"lint": "biome check . --write",
		"migrate": "npm run migrate --workspace=apps/migrations",
		"migrate:clear": "npm run clear --workspace=apps/migrations",
		"migrate:production": "npm run migrate:production --workspace=apps/migrations",
		"seed:dev": "npm run seed:dev --workspace=apps/migrations"
	},
	"version": "1.0.0",
	"overrides": {
		"lodash": "4.17.23"
	},
	"workspaces": [
		"apps/backend",
		"apps/frontend",
		"apps/migrations",
		"packages/api-types",
		"packages/common-types",
		"packages/database-types"
	]
}
